{
    "name": "recipe-catalog-api",
    "version": "1.0.0",
    "description": "Express.js приложение для каталога рецептов",
    "main": "src/server.js",
    "scripts": {
        "start": "node src/server.js",
        "dev": "nodemon src/server.js",
        "predeploy": "npm run build",
        "deploy": "gh-pages -d build"
    },
    "dependencies": {
        "cors": "^2.8.5",
        "express": "^4.18.2"
    },
    "devDependencies": {
        "gh-pages": "^6.3.0",
        "nodemon": "^3.0.1"
    }
}
